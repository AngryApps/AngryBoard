import { Routes } from '@angular/router';
import { BoardGroupComponent } from './components/board/board-group/board-group.component';

export const routes: Routes = [{ path: '', component: BoardGroupComponent }];
